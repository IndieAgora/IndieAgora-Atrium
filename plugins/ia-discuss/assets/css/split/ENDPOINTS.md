# assets/css/split endpoint notes

No endpoints are defined in this folder.

These files are stylesheet slices only. They may style elements that are rendered from endpoint responses, but they do not own transport, action names, or query routes.
