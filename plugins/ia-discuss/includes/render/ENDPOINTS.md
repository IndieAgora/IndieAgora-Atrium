# includes/render endpoint notes

This folder does not register WordPress endpoints.

Render classes here are used by endpoint handlers to transform phpBB content into HTML and media markup. Changes here can alter endpoint responses indirectly, but not the endpoint surface itself.
